<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - CSE Motors</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .welcome-message {
            font-size: 1.5rem;
            color: #333;
            margin: 20px 0;
        }
        .management-section {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }
        .update-section {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
        }
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background: #1a237e;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            margin: 10px 5px;
        }
        .btn:hover {
            background: #283593;
        }
        .btn-primary {
            background: #3949ab;
        }
        .messages {
            background: #e3f2fd;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }
        .notice {
            color: #1976d2;
            margin: 5px 0;
        }
    </style>
</head>
<body>
    <%- include('../partials/header', { nav }) %>
    
    <main class="container">
        <h1>Account Management</h1>
        
        <% if (messages && messages.length > 0) { %>
            <div class="messages">
                <% messages.forEach(message => { %>
                    <p class="notice"><%= message %></p>
                <% }); %>
            </div>
        <% } %>
        
        <!-- Greeting based on account type (Task 3) -->
        <%- greeting %>
        
        <!-- Inventory Management for Employees/Admins -->
        <%- inventoryManagement %>
        
        <!-- Update account link for all users -->
        <div class="update-section">
            <h3>Update Account Information</h3>
            <p>
                <a href="/account/update/<%= account_id %>" class="btn">
                    Update Account Information
                </a>
            </p>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
</body>
</html>